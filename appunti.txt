#arch xfce4

#xfce4
pacman -S xfce4 xfce4-dev-tools xorg-server-xephyr xfce4-screenshooter xfce4-terminal xarchiver thunar-archive-plugin transmission-gtk xfce4-battery-plugin xfce4-datetime-plugin xfce4-notifyd xfce4-pulseaudio-plugin xfce4-whiskermenu-plugin gthumb galculator mousepad gparted redshift gvfs gnome-keyring --noconfirm

#lightdm
pacman -S --needed lightdm lightdm-gtk-greeter lightdm-slick-greeter --noconfirm
systemctl enable lightdm
sed -i 's/#greeter-session=example-gtk-gnome/greeter-session=lightdm-slick-greeter/g' /etc/lightdm/lightdm.conf

__

#Debian gnome SW center error

fwupdmgr --force refresh

__

#vs code
#wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
#install -o root -g root -m 644 packages.microsoft.gpg /etc/apt/trusted.gpg.d/
#sh -c 'echo "deb [arch=amd64,arm64,armhf signed-by=/etc/apt/trusted.gpg.d/packages.microsoft.gpg] https://packages.microsoft.com/repos/code stable main" > /etc/apt/sources.list.d/vscode.list'
#rm -f packages.microsoft.gpg
#apt update
#apt install code -y

__

#arch kde

#kde
pacman -S --needed plasma plasma-wayland-session plasma-wayland-protocols sddm xarchiver thunar thunar-archive-plugin konsole okular galculator transmission-qt kate spectacle packagekit-qt5 print-manager system-config-printer ksystemlog partitionmanager kamoso gwenview kwallet-pam kwalletmanager --noconfirm

#remove components
pacman -Rd --nodeps bluez bluedevil plasma-browser-integration plasma-firewall --noconfirm

#sddm
systemctl enable sddm
